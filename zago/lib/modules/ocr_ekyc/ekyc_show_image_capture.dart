import 'package:flutter/material.dart';

class ShowImageCapture extends StatelessWidget {
  const ShowImageCapture({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
